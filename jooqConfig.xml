<configuration xmlns="http://www.jooq.org/xsd/jooq-codegen-3.14.0.xsd">
  <!-- Configure the database connection here -->
  <jdbc>
    <driver>org.sqlite.JDBC</driver>
    <url>jdbc:sqlite:MyJournalDB.db</url>
    <username></username>
    <password></password>
  </jdbc>

  <generator>
    <database>
      <name>org.jooq.meta.sqlite.SQLiteDatabase</name>
      <includes>.*</includes>

      <!-- All elements that are excluded from your schema (A Java regular expression.
           Use the pipe to separate several expressions). Excludes match before
           includes, i.e. excludes have a higher priority -->
      <excludes>
           UNUSED_TABLE                # This table (unqualified name) should not be generated
         | PREFIX_.*                   # Objects with a given prefix should not be generated
         | SECRET_SCHEMA\.SECRET_TABLE # This table (qualified name) should not be generated
         | SECRET_ROUTINE              # This routine (unqualified name) ...
      </excludes>

      <inputSchema></inputSchema>
    </database>
    <generate/>

    <target>
      <packageName>org.jooq.com.vellu.journaling101</packageName>
      <directory>/home/vellu/NetBeansProjects/mavenproject1/journaling101/src/main/java/com/vellu/journaling101</directory>
    </target>
  </generator>
</configuration>